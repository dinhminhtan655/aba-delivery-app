<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".main.home.fragmentchuyenxe.CheckStopFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcStop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignTop="@+id/btnClose"
        android:paddingBottom="@dimen/dp70"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_check_store"
        />

    <Button
        android:id="@+id/btnClose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/dp30"
        app:layout_anchor="@id/rcStop"
        app:layout_anchorGravity="bottom|left"
        android:text="Đóng"/>

    <Button
        android:id="@+id/btnRefresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="@dimen/dp30"
        app:layout_anchor="@id/rcStop"
        app:layout_anchorGravity="bottom|right"
        android:text="Làm mới"/>


</androidx.coordinatorlayout.widget.CoordinatorLayout>